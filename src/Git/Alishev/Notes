1. Команда создания гит-репозитория в папке: git init
1? Команда для создания нового гит репозитория?

2. Конфигурация добавление имени: git config --global user.name “Имя Фамилия”
2? Комнада для добавления в конфигурацию имени пользователя

3. Конфигурация добавление email: git config --global user.email “Ваш email”
3? Команда для добавления в конфигурацию почты пользователя?

4. Команда просмотра изменений(комитов): git log
4? Команда для просмотра комитов?

5. Различные форматы команды add:
   git add <список файлов> - git add ﬁle1 ﬁle2
   git add . - добавить все файлы в текущей папке
   git add *.java - добавить все файлы в текущей папке с расширением .java
   git add someDir/*.java - добавить все файлы в папке someDir с расширением .java
   git add someDir/ - добавить все файлы в папке someDir
   git add “*.java” - добавить все файлы в проекте с расширением .java
5? Синтаксим команды add?

6. Команда git diff: показывает изменения между текущим неотслеживаемым состоянием (до команды git add) и последним комитом
6? Что делает команда git diff?

7. Команда git diff --staged: показывает изменения между текущим отслеживаемым состоянием (до команды git add) и последним комитом
7? Что делает команда git diff --staged?

8. Команда git diff commit_id: показывает изменения между текущим  состоянием репозитория и указанным комитом
8? Что делает команда git diff commit_id?

9. git reset служит для отката изменений в проекте к какому либо комиту.
9? Для чего предназначена комманда git reset?

10. HEAD обычно указывает на последний (актуальный) комит.
10? На  что обычно указывает HEAD?

11. HEAD можно смещать: HEAD^^ == HEAD~2 означает смещение на 2 комита назад (^ - на 1, ^^-на 2 итд.)
11? Что означает HEAD^^ ,  HEAD~2 ?

12. У reset есть параметры --soft - перевод коммитов после указнного в команде reset в staged зону, --mixed - в unstaged зону, --hard - удаление комитов.
12? Что означает параметр --soft у команды reset? --mixed? --hard?

13. git reset --hard HEAD^^ - удалит комиты после того, к которому переместится HEAD
13? Какие комиты удалит команда git reset --hard HEAD^^ ?

14. git reset == git reset --mixed HEAD, если не указывать параметр по умолчанию используется --mixed, если не указывать комит по умолчанию будет HEAD
14? Какой параметр по умолчанию используется в команде reset --soft, --mixed или --hard?

15. master -это ваш местный филиал, origin master -это ветвь master в удаленном репозитории с именем origin, origin/master -это ваша локальная копия origin master
15? Что такое master, origin master, origin/master?

16. Нельзя удалять коммиты (git reset --hard) если они были опубликованы в публичном репозитории
16? Чего нельзя делать после публикации в публичный репозиторий?

17. git checkout служит для перемещения между комитами, версиями отдельных файлов и ветками
17? Для чего служит команда checkout?

18. git checkout переводит указатель HEAD в состояние DETACHED, в этом состоянии все изменения внесенные в проект не сохраняются.
18? Что происходит с HEAD при переходе на комит отличный от актуального с помощью checkout? Что происходит с изменениями вносимыми в проект в состоянии DETACHED HEAD?

19. git checkout master переход к последнему комиту (master название текущей ветки)
19? Как вернуться к актуальному комиту с помощью checkout?

20. Команда git checkout commit_id -- fileName или git checkout commit_id -- . возвращает fileName или все файлы (.) к версии которая была в commit_id. Действует для unstaged изменений.

21. Команда git checkout -- fileName или git checkout -- . возвращает fileName или все файлы (.) к версии которая была в последнем актуальном комите (HEAD). Действует для unstaged изменений.


///////////////////////ВОПРОСЫ//////////////////
1? Команда для создания нового гит репозитория?
2? Комнада для добавления в конфигурацию имени пользователя
3? Команда для добавления в конфигурацию почты пользователя?
4? Команда для просмотра комитов?
5? Синтаксим команды add?
6? Что делает команда git diff?
7? Что делает команда git diff --staged?
8? Что делает команда git diff commit_id?
9? Для чего предназначена комманда git reset?
10? На  что обычно указывает HEAD?
11? Что означает HEAD^^ ,  HEAD~2 ?
12? Что означает параметр --soft у команды reset? --mixed? --hard?
13? Какие комиты удалит команда git reset --hard HEAD^^ ?
14? Какой параметр по умолчанию используется в команде reset --soft, --mixed или --hard?
15? Что такое master, origin master, origin/master?
